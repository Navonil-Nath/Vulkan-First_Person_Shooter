#ifndef APP_TEST_RUNNER
#define APP_TEST_RUNNER

class AppTestRunner{
    public:
        AppTestRunner();
        ~AppTestRunner();

        AppTestRunner(const AppTestRunner&)=delete;
        AppTestRunner& operator=(const AppTestRunner&)=delete;

    private:


};

#endif